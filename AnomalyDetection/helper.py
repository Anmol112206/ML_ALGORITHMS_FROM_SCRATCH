#X[:, 0]: Extracts all values of the first feature (x-axis).
#X[:, 1]: Extracts all values of the second feature (y-axis).

#X[anomalies, 0]: From the full dataset X, it selects only the anomalous rows for the first feature.